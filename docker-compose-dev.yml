version: '3.8'

services:
  db:
    image: postgres
    env_file:
      - ./backend/.env
    volumes:
      - ${DB_VOLUME}:/var/lib/postgresql/data
    environment:
      - POSTGRES_DB=sgceac
      - POSTIGRES_HOST=db
      - POSTGRES_PASSWORD='$$z$_xs-!@*wyq6&ewf38rtjl#!5-obs*8mtdrpov%zq$_91w6'
      - POSTGRES_ROOT_PASSWORD='2vb&voihgu*9+0m#6(tyzkkm+vi=_s!_d-^osug5*2a%2o(+5)'
    expose:
      - 5432
    ports:
      - 5432:5432
    networks:
      - net
    restart: unless-stopped

  adminer:
    image: adminer
    env_file:
      - ./backend/.env
    ports:
      - 8080:${ADMINER_EXPOSE_PORT}
    networks:
      - net
    restart: unless-stopped

networks:
  net:
    driver: bridge
