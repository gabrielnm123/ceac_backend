version: '3.8'

services:
  gunicorn:
    build: ./backend
    working_dir: /app
    volumes:
      - ./backend/staticfiles:/staticfiles
    environment:
      - GUNICORN_EXPOSE_PORT=${GUNICORN_EXPOSE_PORT}
    entrypoint: bash entrypoint.sh
    expose:
      - ${GUNICORN_EXPOSE_PORT}
    networks:
      - net
    depends_on:
      - db
    stdin_open: true
    tty: true
    restart: unless-stopped

  nginx:
    build: ./backend/nginx
    volumes:
      - ./backend/staticfiles:/staticfiles
    env_file:
      - ./backend/.env
    ports:
      - ${NGINX_PORT}:${NGINX_EXPOSE_PORT}
    networks:
      - net
    depends_on:
      - gunicorn
    restart: unless-stopped

  db:
    image: postgres
    env_file:
      - ./backend/.env
    volumes:
      - ${DB_VOLUME}:/var/lib/postgresql/data
    environment:
      - POSTGRES_DB=${DB}
      - POSTGRES_USER=${DB_USER}
      - POSTGRES_PASSWORD=${DB_PASSWORD}
      - POSTGRES_ROOT_PASSWORD=${DB_ROOT_PASSWORD}
    expose:
      - ${DB_EXPOSE_PORT}
    networks:
      - net
    restart: unless-stopped

  adminer:
    image: adminer
    ports:
      - ${ADMINER_PORT}:${ADMINER_EXPOSE_PORT}
    networks:
      - net
    restart: unless-stopped

networks:
  net:
    driver: bridge

volumes:
  staticfiles:
